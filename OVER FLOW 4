#include <stdio.h>
#include <string.h>

void vulnerable(char *input) {
    char buffer[10];
    strcpy(buffer, input);
    printf("%s\n", buffer);
}

int main(int argc, char *argv[]) {
    vulnerable(argv[1]);
    return 0;
}

#COMMANDS 
gcc -fno-stack-protector -z execstack -o overflow overflow.c
./overflow hello
./overflow AAAAAAAAAAAAAAAAAAAAAAAA
